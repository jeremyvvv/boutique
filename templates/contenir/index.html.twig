{% extends 'base.html.twig' %}

{% block title %}Contenir index{% endblock %}

{% block body %}
    <h1>Contenir index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for contenir in contenirs %}
            <tr>
                <td>{{ contenir.id }}</td>
                <td>{{ contenir.quantite }}</td>
                <td>
                    <a href="{{ path('contenir_show', {'id': contenir.id}) }}">show</a>
                    <a href="{{ path('contenir_edit', {'id': contenir.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('contenir_new') }}">Create new</a>
{% endblock %}
