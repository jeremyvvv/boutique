{% extends 'base.html.twig' %}

{% block title %}Produit index{% endblock %}

{% block body %}
    <h1 class="text-center">Les meilleurs fauteuils du monde sur cette page</h1>

    <div class="row">
        {% for p in produits %}
            <div class="card" style="width: 18rem;">
                <img class="card-img-top" src="{{ asset('')~p.image }}" alt="Card image cap">
                <div class="card-body">
                    <h5 class="card-title">{{ p.libelle }}</h5>
                    <h6 class="card-subtitle mb-2 text-muted">{{ p.tarif }}€</h6>
                    <p class="card-text">{{ p.description }}</p>
                    <a href="{{ path('produit_show', {'id': p.id}) }}" class="btn btn-primary ">Cliquer ici ne fera rien du tout</a>
                </div>
            </div>
        {% endfor %}
    </div>

    <a href="{{ path('produit_new') }}">Create new</a>
{% endblock %}
